{
  "name": "auto-split-e2e",
  "version": "1.0.0",
  "description": "End-to-end tests for auto-split application",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "test:debug": "playwright test --debug"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}